// take an array of arrays containing the numbers zero or one, representing a maze
// in which the acceptable locations are zeroes, the walls are ones, the upper
// left corner is a zero and is the start, and the lower right corner is a zero
// and is the finish.  Allowed moves are horizontal and vertical.

var findSolution = function(maze){
  var X = maze.length;
  var Y = maze[0].length;
  
}